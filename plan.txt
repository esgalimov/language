сделать оперативную память:
    массив интов в процессоре
    команды вида pop [1+ax]
                 push [-5+ax]
каждой переменной будет присваиваться своя ячейка в памяти и будут происходить действия с ней с помощью pop и push
думаю пока обойтись без локальных переменных

для меток функций использовать их индекс в таблице имен, потом, если останется время:

сделать метки вида .метка, для этого сделать новой метке присваивать номер и делать проверку на существование (дополнительно)

еще разобраться с различными if и else; возможно придется сразу сделать метки со словом (разобраться)

поменять на cpu_t
